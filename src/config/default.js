/**
 *  @type { import('../types/types.js').GlobalConfiguration }
 */
export const DefaultGlobalConfiguration = {
    diagnostic: {
        enable: true,
        size: 50,
        visibility: 'always'
    },
    mode: 'dev',
    strict: false,
    verbose: true
}
